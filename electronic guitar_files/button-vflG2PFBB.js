// Generated by CoffeeScript 1.7.1
define(['external/react', 'external/underscore'], function(React, $u) {
  var button_gen, d;
  d = React.DOM;
  button_gen = function(tag) {
    return React.createClass({
      render: function() {
        var button, className, importance;
        className = '';
        importance = this.props.importance || "primary";
        className += " button-" + importance;
        if (this.props.variant && this.props.variant !== "standard") {
          className += " button-" + this.props.variant;
        }
        if (this.props.className) {
          className += " " + this.props.className;
        }
        button = this.transferPropsTo(d[tag]({
          className: className
        }, this.props.children));
        return button;
      }
    });
  };
  return {
    button: button_gen("button"),
    link_button: button_gen("a")
  };
});

//# sourceMappingURL=button.map
